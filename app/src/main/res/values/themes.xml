<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DesignUI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_black</item>
        <item name="colorPrimaryVariant">@color/blue_black</item>
        <item name="android:statusBarColor">@color/blue_black</item>
    </style>

    <style name="Theme.DesignUI" parent="Base.Theme.DesignUI"/>
</resources>